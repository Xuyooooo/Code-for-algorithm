

#include <bits/stdc++.h>
using namespace std;
int q;
int main()
{
    string a;
    cin >> a;
    cin >> q;
    for (int i = 1; i <= q; i++)
    {
        string x, y;
        cin >> x >> y;
        int pos = a.find(x);
        for (int i = pos + x.size(); i < a.size(); i++)
        {
            if (a[i] ==)
        }
    }
}